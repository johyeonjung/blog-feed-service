<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.johyeonjung.blogfeed.mapper.PostImageMapper">

<insert id="insert" useGeneratedKeys="true" keyProperty="postImageId">
    INSERT INTO post_image_tb (
    post_id,
    image_url,
    image_order
    )
    VALUES (
    #{postId},
    #{imageUrl},
    #{imageOrder}
    )
</insert>

<select id="getImageUrlsByPostId" resultType="string">
    select image_url
        from post_image_tb
        where post_id = #{postId}
        order by image_order asc
</select>
</mapper>
