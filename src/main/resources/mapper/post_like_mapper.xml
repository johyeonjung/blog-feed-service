<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.johyeonjung.blogfeed.mapper.PostLikeMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="postLikeId" >
        INSERT INTO post_like_tb (
        post_id,
        user_id
        )
        VALUES (
        #{postId},
        #{userId}
        )
    </insert>

    <select id="exists" resultType="int">
        select count(*)
        from post_like_tb
        where post_id = #{postId}
        and user_id = #{userId}
    </select>


    <delete id="delete">
        delete from post_like_tb
        where post_id = #{postId}
        and user_id = #{userId}
    </delete>

</mapper>
